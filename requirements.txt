# The unofficial NotebookLM client. No browser extra is needed at runtime because
# the CLI reads the seeded session from NOTEBOOKLM_AUTH_JSON. Pinned to the
# version verified against this worker's CLI calls.
notebooklm-py==0.7.3
